[pytest]
pythonpath = tests
asyncio_mode = auto
testpaths = tests
markers =
    unit: Fast unit tests with all I/O mocked
    integration: Real integration tests hitting LLM, MongoDB, Milvus, DuckDB (deselect with '-m "not integration"')
